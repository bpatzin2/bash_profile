# To use this, add "source ~/code/bash_profile/.bash_profile" to the top of ~/.profile (or .bash_profile, not sure of the difference)

# git
alias g='git add -A && git commit -m'
alias ga="git add -A"
alias gc="git commit -m"
alias gpull="git pull"
alias gs="git status"
